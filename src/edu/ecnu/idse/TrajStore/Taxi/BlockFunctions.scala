package edu.ecnu.idse.TrajStore.Taxi

/**
  * Created by zzg on 16-2-16.
  */
/*lass BlockFunctions(rdd : RDD[Block]) extends RDD[Block]{

  override def compute(split:Partition, context:TaskContext): Iterator[Block] ={
    firstParent[Block].iterator(split,context).map(blockRecord =>{

    })
  }

  def getIndex(): Unit ={

  }
}

object BlockFunctions{

}*/